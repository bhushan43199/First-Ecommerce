import { SrtingDirectiveDirective } from './srting-directive.directive';

describe('SrtingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SrtingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
